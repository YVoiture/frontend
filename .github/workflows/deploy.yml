name: Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Vercel Deployment
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: TzSvxbGKe8ASFi8uE87eN76H
          vercel-org-id: team_AIoGlYXtBaCVQY5GieyNjwVP
          vercel-project-id: prj_oGO1iN8hQgmIdd6AX5OawAWIbdRX
          vercel-args: '--prod'